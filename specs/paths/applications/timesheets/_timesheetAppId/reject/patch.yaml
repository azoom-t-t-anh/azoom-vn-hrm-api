operationId: /applications/timesheets/{timesheetAppId}/reject/
tags:
  - Applications
summary: PATCH /applications/timesheets/{timesheetAppId}/reject/
description: Reject to application
parameters:
  - timesheetAppId: page
    in: path
    description: Timesheet application ID
    example: azoom0019
    schema:
      type: string
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: object
          properties:
            message:
              type: string
          example: { "message": "Reject successfully." }
  400:
    $ref: "#/components/responses/400"
  401:
    $ref: "#/components/responses/401"
  500:
    $ref: "#/components/responses/500"
