operationId: timesheets.checkout
tags:
  - Timesheets
summary: Checkout for time end working.
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: object
          properties:
            message:
              type: string
              description: Notification about checkout.
              example: Checkout successfully at 17:15. || You have not checked in yet.
  400:
    $ref: "#/components/responses/400"
  401:
    $ref: "#/components/responses/401"
  500:
    $ref: "#/components/responses/500"
