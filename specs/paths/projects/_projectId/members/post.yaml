operationId: projects.createMember.
tags:
  - Projects
summary: POST /projects/{projectId}/members/
description: Add member to project
parameters:
  - name: projectId
    in: path
    description: Project ID
    schema:
      type: string
requestBody:
  content:
    application/json:
      schema:
        properties:
          memberId:
            type: string
            example: "azoom002"
          isActive:
            type: integer
            description: Status of member in the project,0- inactive,1- active. Default is 1.
          position:
            type: array
            description: joining Process
            items:
              type: object
              properties:
                positionScore:
                  type: integer
                  description: 1- project leader, 0- member of project
                start:
                  type: string
                  format: date
                  description: Date of participation in the project
                end:
                  type: string
                  format: date-time
                  description: Date leaving the project
            example:
              [
                {
                  "positionScore": 1,
                  "start": "2020-04-06",
                  "end": "2020-04-08",
                },
              ]
        required:
          - memberId
          - position
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: object
          properties:
            memberId:
              type: string
            projectId:
              type: string
            position:
              type: array
            createdUserId:
              type: string
            isActive:
              type: boolean
            createdDate:
              type: string
              format: date-time
            updatedDate:
              format: date-time
          example:
            {
              "memberId": "azoom001",
              "position": [
                  {
                      "positionScore": 1,
                      "start": "2020-04-06",
                      "end": "2020-04-08"
                  }
              ],
              "isActive": true,
              "createdUserId": "azoom0019",
              "createdDate": "2020/04/23 10:35:58",
              "updatedDate": "",
              "fullName": "nguyen van A",
              "projectId": "project-009"
            }
  400:
    $ref: "#/components/responses/400"
  401:
    $ref: "#/components/responses/401"
  500:
    $ref: "#/components/responses/500"
