operationId: /projects/member/
tags:
  - Projects
summary: POST /member/
description: Add member to project
requestBody:
  content:
    application/json:
      schema:
        properties:
          projectId:
            type: string
            description: Input project ID
            example: "hr001"
          memberId:
            type: string
            description: Input user ID of member, who is a project participant.
            example: "0008"
          isJoining:
            type: boolean
            description: Input status joining of member. flase - not joining, true - in joining
            example: true
          managerId:
            type: string
            description: Input ID of project manager.
            example: "azoom0069"
        required:
          - projectId
          - memberId
          - isJoining
          - managerId
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: object
          properties:
            id:
              type: string
            projectId:
              type: string
            memberId:
              type: string
            isJoining:
              type: boolean
            createdUserId:
              type: string
            isActive:
              type: boolean
            created:
              type: string
              format: date-time
            updated:
              format: date-time
          example:
            {
              "projectId": "azoom006979",
              "memberId": "azoom0060839",
              "id": "azoom006979_azoom006083920200330",
              "isJoining": true,
              "isActive": true,
              "createdUserId": "azoom009",
              "created": "2020/03/30 14:58:16",
              "updated": "",
            }
  400:
    $ref: "#/components/responses/400"
  401:
    $ref: "#/components/responses/401"
  500:
    $ref: "#/components/responses/500"
