operationId: /projects/
tags:
  - Projects
summary: POST /projects/
description: Create new project.
requestBody:
  content:
    application/json:
      schema:
        properties:
          id:
            type: string
            description: Input project ID
            example: "azoom006979"
          projectName:
            type: string
            description: Input projectName.
            example: "azoom-hr-vn"
          status:
            type: integer
            description: Input status of project. -1- pending,0-in progress, 1- done
            example: 0
          managerId:
            type: string
            description: Input of project manager ID.
            example: "azoom009"
        required:
          - id
          - projectName
          - managerId
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: object
          properties:
            id:
              type: string
            projectName:
              type: string
            status:
              type: integer
            managerId:
              type: string
            isActive:
              type: boolean
            created:
              type: string
              format: date-time
            updated:
              format: date-time
          example:
            {
              "id": "azoom006979",
              "projectName": "azoom-hr-vn",
              "managerId": "azoom0069",
              "createdUserId": "azoom009",
              "status": -1,
              "isActive": true,
              "created": "2020/03/30 14:24:50",
              "updated": "",
            }
  400:
    $ref: "#/components/responses/400"
  401:
    $ref: "#/components/responses/401"
  500:
    $ref: "#/components/responses/500"
